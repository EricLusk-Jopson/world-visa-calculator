/**
 * Shared UI constants used across the component tree.
 * Import these instead of hard-coding the values inline.
 */

/** Standard easing curve used for most interactive transitions. */
export const STANDARD_TRANSITION = "all 0.15s cubic-bezier(0.16, 1, 0.3, 1)";

/** Focus-ring box-shadow applied to focused input fields and selects. */
export const FOCUS_RING_SHADOW = "0 0 0 3px rgba(12,30,60,0.06)";

/** Elevation shadow for modal dialogs. */
export const DIALOG_BOX_SHADOW = "0 12px 40px rgba(12,30,60,0.18)";

/** Border-radius for modal dialog paper. */
export const DIALOG_BORDER_RADIUS = "20px";

/** Shared border width for input fields and outlines. */
export const INPUT_BORDER_WIDTH = 1.5;
