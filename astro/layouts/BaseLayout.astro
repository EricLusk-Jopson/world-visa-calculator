---
export interface Props {
  title?: string;
  description?: string;
}

const {
  title = "Schengen Visa Calculator — Track 90/180 Days for Multiple Travelers",
  description = "Free Schengen calculator for couples, families and groups. Track each person's 90-day allowance with a visual timeline, overstay warnings, and shareable links. No account needed.",
} = Astro.props;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <!-- ── Favicon ──────────────────────────────────────────────────────── -->
    <link rel="icon" type="image/svg+xml" href="/geo-dark.svg" />
    <link rel="apple-touch-icon" href="/apple-touch-icon.png" />

    <!-- ── Primary SEO ──────────────────────────────────────────────────── -->
    <title>{title}</title>
    <meta name="description" content={description} />
    <meta name="robots" content="index, follow" />
    <link rel="canonical" href="https://eurovisacalculator.com/" />

    <!-- ── Open Graph (Facebook, LinkedIn, WhatsApp previews) ───────────── -->
    <meta property="og:type" content="website" />
    <meta property="og:url" content="https://eurovisacalculator.com/" />
    <meta
      property="og:title"
      content="Schengen Visa Calculator — Track 90/180 Days for Multiple Travelers"
    />
    <meta
      property="og:description"
      content="Free Schengen calculator for couples, families and groups. Track each person's 90-day allowance with a visual timeline, overstay warnings, and shareable links."
    />
    <meta
      property="og:image"
      content="https://eurovisacalculator.com/og-image.png"
    />
    <meta property="og:image:width" content="1200" />
    <meta property="og:image:height" content="630" />
    <meta property="og:locale" content="en_US" />
    <meta property="og:site_name" content="EuroVisaCalculator" />

    <!-- ── Twitter / X card ─────────────────────────────────────────────── -->
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:url" content="https://eurovisacalculator.com/" />
    <meta
      name="twitter:title"
      content="Schengen Visa Calculator — Track 90/180 Days for Multiple Travelers"
    />
    <meta
      name="twitter:description"
      content="Free Schengen calculator for couples, families and groups. Visual timeline, overstay warnings, shareable links."
    />
    <meta
      name="twitter:image"
      content="https://eurovisacalculator.com/og-image.png"
    />

    <!-- ── Structured data (Google rich results) ────────────────────────── -->
    <script type="application/ld+json">
      {
        "@context": "https://schema.org",
        "@type": "WebApplication",
        "name": "EuroVisaCalculator",
        "url": "https://eurovisacalculator.com",
        "description": "Free Schengen 90/180-day visa tracker for multiple travelers. Track allowances, plan trips, and share results instantly.",
        "applicationCategory": "TravelApplication",
        "operatingSystem": "Any",
        "offers": {
          "@type": "Offer",
          "price": "0",
          "priceCurrency": "USD"
        }
      }
    </script>

    <!-- ── Fonts ─────────────────────────────────────────────────────────── -->
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Fraunces:ital,opsz,wght@0,9..144,400;0,9..144,600;1,9..144,400&family=Plus+Jakarta+Sans:wght@400;500;600&display=swap"
      rel="stylesheet"
    />
  </head>
  <body>
    <slot />
  </body>
</html>
